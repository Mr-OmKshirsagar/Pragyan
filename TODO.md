# Pragyan — Interactive Roadmap Learning System

## Plan steps
1. Add routing for `/roadmap/*` using `react-router-dom`.
2. Create `topicCatalog` data for all requested domains/topics with learning workspace content.
3. Implement `RoadmapTopicCatalogPage` with clickable glass topic cards.
4. Implement `TopicLearningWorkspacePage` (hero, modules, checklist, resources, practice/projects, quiz, analytics, notes, continue learning).
5. Implement localStorage-backed progress + quiz results + notes.
6. Verify UI responsiveness + premium glassmorphism styling consistency.
7. Run frontend dev server and smoke test navigation + progress persistence.

